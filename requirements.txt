# requirements.txt ─ runtime dependencies for the “Eye for Blind” Colab notebook
# Target environment: Python 3.8.10, TensorFlow 2.10.0 (+nv22.11) on 4×RTX 5000 GPUs

tensorflow==2.10.0           # unified CPU/GPU wheel; matches nv22.11 build on Colab
tensorflow-datasets==4.9.4    # convenient access to Flickr-30K (or other TFDS image sets)

numpy==1.23.5                 # last NumPy release officially tested with TF 2.10
matplotlib==3.9.0             # for visualisations & attention heat-maps
Pillow==10.3.0                # image I/O and basic processing
tqdm==4.66.4                  # neat CLI progress bars

nltk==3.8.1                   # BLEU-1‒4 metrics (nltk.translate.bleu_score)
gTTS==2.5.1                   # Google-TTS for caption-to-speech demo
